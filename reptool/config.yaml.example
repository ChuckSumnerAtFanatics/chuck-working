logging:
  level: WARNING  # Can be DEBUG, INFO, WARNING, ERROR, or CRITICAL

output:
  default_format: json
  json_style: monokai  # Default style for JSON syntax highlighting

monitoring:
  max_workers: 10  # Maximum number of concurrent threads for processing hosts
